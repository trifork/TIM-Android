publishing {
    publications {
        release(MavenPublication) {
            groupId = 'com.github.trifork'
            artifactId = 'TIM-Android'
            version = versionName
            afterEvaluate {
                from components.release
            }
        }
    }
}
