afterEvaluate {

    publishing {
        publications {
            release(MavenPublication) {
                groupId = 'com.github.trifork'
                artifactId = 'TIM-Android'
                version = versionName
                from components.release
            }
//            artifact("$buildDir/libs/TIM-Android-lib-sources.jar")
        }
    }
}